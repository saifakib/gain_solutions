# gain_solutions

## Start Server

```
> cd server
> npm install
```

Create a .env file in root directory and setup `DATABASE` address and `PORT` number.
For Developemt stage type command:
```
npm run dev
``` 
Or Production stage for 
```
npm run prod
```
Then go for starting client react server
```
cd ..
cd client
npm install
npm start
```
